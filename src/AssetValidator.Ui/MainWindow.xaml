<Window x:Class="AssetValidator.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AssetValidator.Ui.Controls"
        Title="Asset Validator"
        Height="520"
        Width="900"
        Icon="Assets/Icon.ico"
        Background="#1E1E1E"
        Foreground="#E0E0E0"
        FontFamily="Segoe UI">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:HeaderView Grid.Row="0"
                             Margin="0, 0, 0, 10"
                             OnValidationRequired="ValidateJsonAssets" />
        
        <controls:FiltersView Grid.Row="1"
                              Margin="0, 0, 0, 10"
                              OnFiltersChanged="RefreshUi" />

        <controls:ResultsView Grid.Row="2"
                              ItemsSource="{Binding ValidationResults}" />
    </Grid>
</Window>