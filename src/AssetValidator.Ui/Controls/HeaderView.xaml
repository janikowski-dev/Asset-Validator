<UserControl x:Class="AssetValidator.Ui.Controls.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border DockPanel.Dock="Top"
            Background="#252526"
            Padding="12"
            CornerRadius="6">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Asset Validator"
                           FontSize="18"
                           FontWeight="SemiBold" />

                <TextBlock Text="Validate exported asset metadata against project rules"
                           FontSize="12"
                           Foreground="#A0A0A0"
                           Margin="0,2,0,0" />
            </StackPanel>

            <Button Grid.Column="1"
                    Cursor="Hand"
                    FontSize="13"
                    Foreground="#FFFFFF"
                    Background="Transparent"
                    BorderThickness="0"
                    VerticalAlignment="Center"
                    Click="RequireValidation">

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="#1E1E1E"
                                BorderBrush="#3A3A3A"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="8, 6">

                            <TextBlock Text="Validate"
                                       Foreground="{TemplateBinding Foreground}" />

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg"
                                        Property="Background"
                                        Value="#2A2A2A" />
                                <Setter TargetName="Bg"
                                        Property="BorderBrush"
                                        Value="#4A4A4A" />
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bg"
                                        Property="Background"
                                        Value="#161616" />
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground"
                                        Value="#777" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>

            </Button>
        </Grid>
    </Border>
</UserControl>